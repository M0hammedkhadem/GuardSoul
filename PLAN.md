# خطة بناء «عقل» تطبيق طريق النقاء — وثيقة معمارية

## 1. نتائج البحث المعمق

### نموذج الذكاء الصناعي (فلتر الصور)
- **المعتمد**: `open_nsfw` (Yahoo — رخصة BSD-2) بصيغة TFLite من مشروع devzwy/open_nsfw_android.
- **الملف**: `app/src/main/assets/nsfw.tflite` (≈ 22.5MB، توقيع TFL3 سليم).
- **المواصفات**: مدخل `[1,224,224,3] float32` بترتيب **BGR** مع طرح متوسطات Caffe
  (B−104, G−117, R−123)، والمخرج `[1,2]` = (آمن، غير لائق).
- **التشغيل**: TensorFlow Lite 2.17.0 محليًا 100% — لا تغادر أي صورة الجهاز.
- **العتبة**: حجب عند درجة NSFW ≥ 0.85 (توازن دقة/إنذارات كاذبة).

### التقاط الشاشة
- `AccessibilityService.takeScreenshot()` متاح من API 30+ ويتطلب
  `android:canTakeScreenshot="true"` في إعدادات الخدمة. تحت API 30 يُعتمد على
  التحليل الشجري فقط (تدهور رشيق Graceful degradation).
- Throttling صارم: لقطة كل ≥ 700ms للريلز وكل ≥ 2500ms لفحص NSFW.

## 2. معمارية العقل `com.agon.app.engine`

```
ProtectionAccessibilityService (المستشعرات + المنفذ)
 └─ DetectionEngine (غرفة القرار)
     ├─ EngineSettings        ← DataStore مشترك (تحديث لحظي من الواجهة)
     ├─ AppPolicy             ← حظر كامل / حظر مقاطع لكل حزمة معروفة
     ├─ FacebookReelsBrain    ← آليتان متوازيتان (أدناه)
     ├─ GenericShortsDetector ← YouTube Shorts / Instagram Reels (معرفات + أوصاف)
     ├─ BrowserGuard          ← قراءة شريط العنوان، قائمة سوداء/بيضاء، SafeSearch، كلمات
     ├─ NsfwClassifier        ← TFLite open_nsfw (محلي)
     └─ BlockOverlay           ← درع حجب فوق أي تطبيق (TYPE_ACCESSIBILITY_OVERLAY)
```

## 3. عقل ريلز فيسبوك — الآليتان المتوازيتان

### الآلية الأولى: حالة الشريط العلوي للتبويبات (تحليل بكسلي)
ثلاث حالات مُثبتة من لقطات الشاشة:
1. **شريط أبيض** ← أي تبويبة عدا ريلز ← **دليل نفي قاطع**.
2. **شريط أسود** ← داخل تبويبة ريلز ← **دليل إثبات قوي**.
3. **شريط مختفٍ** (تمرير داخل الريلز) ← حياد ← يُحسم بالآلية الثانية.

التنفيذ: أخذ شريحة أفقية أسفل شريط الحالة مباشرة (≈ 56dp)، حساب متوسط
الإضاءة Luminance لعيّنة شبكية من البكسلات:
- متوسط ≥ 200 ونسبة بكسلات فاتحة ≥ 70% ← WHITE
- متوسط ≤ 60 ونسبة بكسلات داكنة ≥ 70% ← BLACK (الفيديو قد يكون داكنًا
  أيضًا، لذا الأسود دليل مرجِّح وليس قاطعًا — يُدمج مع الآلية الثانية)
- خلاف ذلك ← UNKNOWN/HIDDEN

### الآلية الثانية: العمود التفاعلي (تحليل شجرة العقد)
في الريلز توجد 4 أيقونات (إعجاب، تعليق، مشاركة، حفظ) مرصوفة عموديًا
على الحافة (يسار RTL / يمين LTR)، وموقعها الرأسي **غير ثابت** (تصعد/تنزل
بحسب منهج الدخول). الخوارزمية:
1. مسح الشجرة وتصنيف العقد حسب الوصف/النص (مرادفات عربية + إنجليزية
   لـ like/comment/share/save).
2. ترشيح: مركز X ضمن أقصى 22% من إحدى الحافتين، والعقدة في النصف السفلي.
3. تجميع عمودي: محاذاة X بتفاوت ≤ 8% من العرض (يستوعب الانزياح)، ترتيب
   تصاعدي بـ Y مع فجوات منطقية.
4. **القرار: ≥ 3 فئات مختلفة من 4 في العمود ذاته ← ريلز**.

### الدمج ومنع الإنذارات الكاذبة
- WHITE ← إلغاء فوري (لسنا في ريلز).
- BLACK + عمود ≥ 3 ← حظر فوري.
- HIDDEN + عمود ≥ 3 في تقييمين متتاليين خلال ≤ 2.5 ثانية ← حظر (Debounce).
- تصفير الحالة عند تغيّر الحزمة/النافذة.

## 4. بقية الميزات
- **حظر كامل للتطبيقات**: خريطة حزم (إنستغرام/تيكتوك/يوتيوب/فيسبوك/سناب/إكس
  + نسخ Lite) ← عند فتح تطبيق محظور: درع حجب + العودة للرئيسية (HOME).
- **YouTube Shorts / Instagram Reels**: معرفات واجهة (`reel_`, `clips_`, `shorts`)
  + أوصاف نصية ← حجب + رجوع (BACK) لإبقاء المستخدم داخل التطبيق.
- **حارس المتصفح**: قراءة شريط العنوان لكروم/فايرفوكس/إيدج/سامسونج/بريف...
  ← قائمة سوداء (مع استثناء البيضاء) + كلمات مفتاحية + فرض SafeSearch على
  Google/Bing/Yahoo/DuckDuckGo/YouTube عند غياب وسم الأمان.
- **فلتر الصور AI**: عيّنة لقطة كل 2.5ث في التطبيقات الحساسة ← تصغير ← استدلال
  TFLite ← حجب عند تجاوز العتبة.
- **الإحصاء**: عداد عمليات الحظر يُحفظ في DataStore المشترك ويظهر لحظيًا في الواجهة.

## 5. الأداء والخصوصية
- كل المعالجة على `Dispatchers.Default`، لا شبكة إطلاقًا في مسار الكشف.
- Throttling للقطات، حد أقصى لعمق مسح الشجرة، إعادة تدوير Bitmaps.
- الخدمة تقرأ الإعدادات من DataStore مشترك واحد (ملف `purity_prefs`) عبر
  Flow — أي تغيير من الواجهة يسري فورًا على العقل دون إعادة تشغيل.
